#ifndef Tad_Montador_H
#define Tad_Montador_H

/*    Contém somente a função que será chamada no main, tem como parâmetros o arquivo de entrada e o arquivo de saida, 
  esta função śerá a responsável por chamar a função que guarda os labels que seria o passo 1, e a função que faz a
  tradução do codigo assembly.*/
void montador(char *arq_entrada,char *arq_saida);

#endif